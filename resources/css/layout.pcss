body {
    height: 100vh;
    display: flex;
    flex-direction: column;

    /* Sidebar on desktop, top nav on mobile */
    @media only screen and (min-width: 992px) {
        flex-direction: row;
    }
}

