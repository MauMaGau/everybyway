.sidenav {
    width: 250px;
    min-height: 100vh;
    padding: var(--gap-3);
    border-right: 1px solid var(--color-grey-4);

    a {
        text-decoration: none;
        display: block;
        user-select: none;
    }

    ul {

    }

    ul ul {
        padding-left: var(--gap-2);
    }
}
